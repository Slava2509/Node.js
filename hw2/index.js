const uuid = require('uuid');
const math = require('./math.js');

const addResult = slMathPackage.add(3, 9);
const subtrResult = slMathPackage.subrtact(3, 2);

console.log(addResult);
console.log(subtrResult);